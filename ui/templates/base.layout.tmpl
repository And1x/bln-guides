{{define "base"}}
<!DOCTYPE html>
<html lang="en">
<head>
    {{/* <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> */}}
    {{with .Guide}}
            {{/* <meta name="lightning" content="lnurlp:{{.Author}}"/> */}}
            <meta name="lightning" content="lnurlp:And1@stacker.news"/>
            <meta property="og:image" content="/static/img/&1.png">
    {{else}}
        <meta name="lightning" content="lnurlp:And1@stacker.news"/>
        <meta property="og:image" content="/static/img/&1.png">

    {{end}}



     <link id="cssstyle" rel="stylesheet" href="/static/css/main.css"> 
    {{/* <link rel="stylesheet" href="/static/css/sys-dark-light.css"> Use sys settings for light/dark mode*/}}

    <title>{{template "title" .}}</title>
</head>
<body>
{{/* <div class="navbar">
    <nav>
        <a href="http://localhost:8080/">₿ Home</a>
        <a href="http://localhost:8080/allguides">Guides</a>
        <a href="http://localhost:8080/createguide">Create</a>
        <div class="ToggleDayNight">
            <a href> <img onclick="jsToggleDayNight()" src="/static/img/ligthdark.png" width=30em> </a>
       </div>
    </nav>
</div> */}}

<div class="navbar">
        <a href="/">₿ Home</a>
        <a href="/allguides">Guides</a>
        {{if .AuthUserId}} {{/* 0 is treated like false */}}
            <a href="/createguide">Create</a>
        {{end}}
        <div class="navbar-right">
            <a href> <img onclick="jsToggleDayNight()" src="/static/img/ligthdark.png" width=30em> </a>
        {{if .AuthUserId}}
            <form action="/user/logout" method="post">
                <input type="hidden" name="csrf_token" value="{{.CSRFToken}}">
                <button class="button-logout">Logout {{.UserName}}</button>
            </form>
            <a href="/user/settings">Settings</a>
        {{else}}
            <a href="/user/register">Register</a>
            <a href="/user/login">Login</a>
        {{end}}
       </div>
</div>
<div>
    <section>
        {{with .FlashMsg}}
            <div class="flash-message">{{.}}</div>
        {{end}}
    </section>
</div>

{{template "body" .}}   

<div class="footer">
<a href="https://www.github.com"><img src="/static/img/GitHub-Mark-32px.png"></a>
</div>

{{block "js" .}}
{{end}}
<script src="/static/js/main.js"></script>
<script>


{{/* todo: refactor day and night mode - im sure this can be done better */}}
{{/* ### Day and night mode ### */}}
var theme = document.getElementById('cssstyle');

if (localStorage.getItem('darkmode') == null || localStorage.getItem('darkmode') == 'true' ) {
    theme.setAttribute('href', '/static/css/main.css');
} else if (localStorage.getItem('darkmode') == 'false') {
    theme.setAttribute('href', '/static/css/day.css');
    console.log('am i here');
    } 

function jsToggleDayNight() {

    if (theme.getAttribute('href') == '/static/css/main.css'){
        localStorage.setItem('darkmode', 'false');
        location.reload();
        console.log(localStorage.getItem('darkmode'));
    } else if (theme.getAttribute('href') == '/static/css/day.css') {
        localStorage.setItem('darkmode', 'true');
        location.reload();
        console.log(localStorage.getItem('darkmode'));
    }
 }
{{/* ### Day and night mode ### */}}
</script>
</body>
</html>
{{end}}